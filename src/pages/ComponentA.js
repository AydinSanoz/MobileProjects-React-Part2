import React from 'react';
import {View, Text} from 'react-native';

const ComponentA = (props) => {
  return (
    <View style={{flex: 1}}>
      <Text>ComponentA</Text>
    </View>
  );
};

export {ComponentA};
